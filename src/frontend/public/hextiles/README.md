Hex tile art vendored from https://github.com/VanDooProject/bg_assets_hextile
(SE camera rotation only — see that repo's `hextiles/` for the other five
rotations and the base/top split). Referenced by
`src/lib/map/textures.ts`.
