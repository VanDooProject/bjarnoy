


<svg:g>

    <text>chunk works! ({{ baseCoordS }}|{{ baseCoordR }}) with tiles {{ tiles.length }}</text>

    <g 
      *ngFor="let tileX of tiles">
      <g app-tile 

      *ngFor="let tile of tileX"
      [tile]="tile"
      >
      </g>
	</g>
    
</svg:g>